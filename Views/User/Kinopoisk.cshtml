 @* @model User

<table class="table">
  <thead>
    <tr>
      <th scope="col">@Model.Id</th>
      <th scope="col">@Model.Name</th>
      <th scope="col">@Model.Year</th>
      <th scope="col">@Model.Country</th>
    </tr>
  </thead>
  <tbody> *@
    @* <tr>
      <th scope="row">@Model.Id</th>
      <td>@Model.Name</td>
      <td>@Model.janr</td>
      <td>@Model.kod</td>
    </tr>
      *@
  </tbody>
</table> 

